
#include <iostream>
using namespace std;
int banknote_number(int n)
{
	if (n % 8 == 0)
	{
		int tmp = n / 8;
		cout << 5 * tmp << " " << 2 * tmp << " " << tmp << endl;
		return 1;
	}
	else
		return 0;
}
int main()
{
	int n;
	cin >> n;
	banknote_number(n);
	return 0;
}
